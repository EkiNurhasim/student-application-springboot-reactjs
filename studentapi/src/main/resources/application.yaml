server:
  port: 4800

spring:
  application:
    name : studentapi
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 122
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update